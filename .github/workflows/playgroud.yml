name: Playground
on: [workflow_dispatch]
jobs:
  job1:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 'scripts'
        shell: bash

    steps:
      - name: run my script
        run: './main.sh'